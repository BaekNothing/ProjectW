# 마법소녀 사무소 M1 버티컬 슬라이스

- Date: 2026-09-22
- Target: Assets/Specification/System/MagicalGirlOfficePrototype.md
- Action: Create
- Scope: 별도 진입 가능한 7일 UI 우선 프로토타입.
- Impact: System, UI, 독립 저장, WebGL 게시.
- SSOT Change: Yes
- Rationale: 사용자가 마이그레이션 계획에 따른 제작과 최초 씬 별도 진입, 재구성 UI 우선 확인을 요청했다.
- Spec Ref: MagicalGirlOfficeMigration M1, MagicalGirlOfficeBlueprint 4–7.

## 적용 경계

기존 캠페인에는 TaskSystem이 계속 적용된다. 새 진입 버튼은 아래의 독립 시나리오를 연다.
수작업 인물·대사·선택 결과는 UI 피드백용 초안이며 장기 게임 밸런스 규칙이 아니다.
M2–M4의 일반 시뮬레이션, 경제 산식, 실제 졸업·멘토 시스템까지 완료한 것으로 보지 않는다.

## 화면과 조작

최초 화면에 ‘마법소녀 사무소 / 7일 프로토타입’ 버튼을 둔다. 2026-09-23 사용자 피드백으로
사이드바 대시보드를 귀여운 가상 OS로 교체한다. 니디걸 오버도즈는 데스크톱 구성의 참고이며
실제 게임 이미지나 로고를 복제하지 않는다. 연보라·분홍·민트, 입체 테두리, 작은 픽셀형 앱 아이콘을 사용한다.
진입 시 열린 창 없이 바탕화면을 보여준다. 오늘의 업무, 일정, 인물 기록, 지역·사건,
메신저, 활동 기록, 설정 아이콘을 한 번 눌러 독립 창을 연다.
여러 창을 동시에 열고 제목줄 이동, 우하단 크기 조절, 최소화·닫기, 작업표시줄 복원,
클릭 시 앞으로 가져오기를 지원한다. 앱당 창은 하나이며 스크롤·선택 상태를 독립적으로 유지한다.
창은 작업 영역 안에 제한하고 최소 크기를 지킨다. 창 배치는 현재 세션 동안 유지한다.
오늘의 안건에서 두 대응안 중 하나를 선택하고 계획 확정으로 다음 날을 진행한다.
선택 이유·결과는 즉시 확인되며 이후 메시지와 활동 기록에 남는다. 미선택 상태로 진행하지 못한다.
7일차 확정 시 주간 정리와 인물별 후속 소식을 표시한다. 타이틀 복귀 및 확인을 거친 다시 시작을 제공한다.
작은 화면에서는 최소 논리 캔버스를 비례 축소하고 긴 창 내용은 스크롤한다.

### 알림과 아이콘 연출

- 우하단 토스트: 첫 진입의 새 안건, 선택 저장, 하루 진행 후 메시지 알림. 7초 후 닫히며
  직접 닫기 또는 관련 앱 열기가 가능하다. 토스트가 사라져도 안건은 앱에서 읽을 수 있다.
- 전체화면 팝업: 하루 확정, 진행 결과, 주간 종료, 초기화 확인. 화면 전체를 덮는 반투명
  레이어와 중앙 창을 사용하고 뒤 창·아이콘·작업표시줄 입력을 막는다. 필수 확인은 자동 닫기하지 않는다.
- 설정에서 팝업/토스트 미리보기 및 이펙트 켜기/끄기를 제공한다.
- 기존 Addressables의 radial-trapezoid, sparkle, ring, glow를 아이콘 뒤 레이어로 공유한다.
  새 안건·선택 아이콘 뒤에서 느린 회전·빛·반짝임을 표현하고 텍스트·클릭 영역을 가리지 않는다.
  리소스 로드 전에도 기본 아이콘은 보이며, 장식이 입력을 가로채지 않는다.
- 향후 앱 아이콘·장식 아이템 교체를 고려해 앱별 도형과 배경 이펙트 그리기를 분리한다.
  이번 범위에는 아이템 획득·상점·장착 등 게임 규칙을 추가하지 않는다.

## 작성된 7일 시나리오

인물 ID는 `office-hana`, `office-sori`, `office-yuna`; 이름은 하나, 소리, 유나.
하나는 학교 발표를 준비하고, 소리는 회복을 요청하며, 유나는 졸업 후 진로를 탐색한다.
사건 ID `office-riverside-01`: 하천 산책로 이상 반응. 학교, 하천, 지원센터를 지역 목록으로 표현한다.

| 일차 | 안건 | 선택 0 | 선택 1 | 후속 |
|---|---|---|---|---|
| 1 | 하나의 발표와 현장 조사 충돌 | 수업 후 조사 예약 | 성인 협력기관에 조사 요청 | 발표 유지, 조사 주체 기록 |
| 2 | 소리의 휴식 요청 | 하루 휴식 확보 | 본인 동의를 받아 지원센터 상담 예약 | 출동 제외, 서로 다른 회복 소식 |
| 3 | 하천 사건 준비 | 유나와 함께 관측 준비 | 전문기관 지원 우선 요청 | 조사 정보와 대응 방침 기록 |
| 4 | 하천 사건 대응 | 유나의 안전 범위 내 관측·주민 안내 | 현장 대응을 기관에 인계 | 직접 전투 없이 사건 안정, 대응 기록 |
| 5 | 유나의 졸업 준비 | 진로 탐색 시간 확보 | 희망하는 인계 자료 함께 정리 | 자동 졸업·멘토 배정 없음 |
| 6 | 사건 후속 점검 | 지역 예방 점검 | 학교·지원기관 연락망 정리 | 이후 일정 여유와 지원 경로 기록 |
| 7 | 한 주 회고 | 각자의 다음 주 약속 정리 | 지원 계획을 함께 검토 | 개인별 기록을 남기고 시나리오 종료 |

모든 경로에서 학교 일정, 휴식 요청과 안전 범위를 지킨다. 출동 횟수나 솔직한 보고를 점수화하지 않는다.
선택에 따른 운영 방식·메시지 차이가 기록된다. 수치 경제·신뢰 자동 변화는 추가하지 않는다.
정보는 확인된 사실/본인 메시지/계획으로 구분하고 일차와 출처를 표시한다.

## 독립 저장과 전이

PlayerPrefs key `projectw.office.m1.v1`, JSON schema `1`, ScenarioId `office-seven-days`.
필드는 SchemaVersion(int), ScenarioId(string), Day(int 1–8), Choices(int[7], -1 미선택/0/1).
Day 8은 종료. Day 이전 선택은 필수, 미래 선택은 -1, 오늘 선택은 -1/0/1.
개별 안건의 영구 ID는 `office-day-1`…`office-day-7`; 배열은 이 고정 콘텐츠 순서를 저장하며 인물 식별에 쓰지 않는다.
선택과 하루 확정 시 저장한다. 불러오기에서 버전·식별자·범위·전이 검증 실패 시 원문을 보존하고
저장을 차단한 상태로 오류를 보여준다. 사용자가 명시적으로 초기화할 때만 해당 프로토타입 키를 지운다.
기존 campaign/desktop 키와 JSON 데이터는 읽거나 변경하지 않는다.

## 작업 TODO와 검증

- 위 화면, 분기, 저장 및 엔딩 구현.
- 정상 7일 진행, 미결정 진행 차단, 종료 후 전이 차단, 불량/미지원 저장 거부에 대한 EditMode 테스트.
- WebGL 빌드 성공과 Butler 업로드/처리 상태를 CLI로 확인. 웹·브라우저·플레이 검증은 하지 않는다.
- 시각 품질과 선택 의미에 대한 판단은 사용자의 직접 확인 이후 기록한다.

## 구현 상태 — 2026-09-23

- WebGL/Editor에서 최초 화면의 별도 버튼, 6개 업무 화면, 7일 분기·종료·독립 저장을 구현했다.
- OfficeScenario / OfficeDesktop 각각의 EditMode 테스트를 추가했다.
- 설치된 Unity 6000.3.8f1 Roslyn 컴파일러와 기존 Editor 참조 목록으로 런타임·테스트 어셈블리
  컴파일 성공. 이는 Unity 테스트 실행이나 WebGL 빌드 성공을 뜻하지 않는다.
- 최초 Unity Editor 시작은 라이선스 오류(exit 198)로 차단됐으나, 사용자의 Hub 재로그인 후 해소됐다.
- Office EditMode 테스트 15개 전체 통과 (`Logs/office-editmode.xml`).
- WebGL 빌드 성공: `WEB_PREVIEW_BUILD_OK bytes=32185939`, 프로세스 exit 0.
  로컬 Addressables catalog/hash와 효과·인물 bundle의 빌드 포함을 확인했다.
- Butler `html5` 게시 처리 완료: build `2006446`, upload `19215817`,
  version `office-m1-20260923-d432bea`. `butler status`에서 성공 표시를 확인했다.
- 브라우저/웹/플레이 검증은 수행하지 않았다. 사용자 시각 피드백은 대기 중이다.
- 이 프로토타입의 신규 런타임은 UNITY_WEBGL 또는 UNITY_EDITOR로 한정한다.
  Android APK/패치/디바이스 채널은 이번 작업의 배포 대상이 아니다.

### 가상 OS 개편 — 2026-09-23

- 바탕화면 기본 진입, 7개 앱 아이콘, 동시 창·이동·크기 조절·최소화·닫기·작업표시줄 복원을 구현했다.
- 창별 스크롤·인물 선택을 분리하고 창 경계와 입력 우선순위를 관리한다.
- 하루 확정/결과/초기화 전체화면 알림, 우하단 토스트, 설정의 연출 미리보기를 추가했다.
- 기존 효과 텍스처 4종을 로더에서 공유해 아이콘 뒤 장식으로 사용한다. 장식 ON/OFF를 제공한다.
- Office 관련 EditMode 27개 전체 통과 (`Logs/office-os-editmode.xml`).
- WebGL 빌드 성공: `WEB_PREVIEW_BUILD_OK bytes=32195502`, 프로세스 exit 0.
- 로컬 catalog/hash와 효과·인물 bundle 포함 확인. Butler 업로드 exit 0.
- 업로드 버전 `office-os-20260923-2889168`, build `2006984`, upload `19215817`.
  업로드 후 `butler status --json`에서 서버 대기열 `queued`를 확인했다. 활성화 완료와 구별한다.
- 웹/브라우저/플레이 검증은 수행하지 않았다. 사용자 시각 피드백을 기다린다.
